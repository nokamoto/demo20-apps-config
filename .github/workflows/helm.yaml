name: Helm

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Check out code into the Helm directory
      uses: actions/checkout@v2
    - name: Make
      run: make
